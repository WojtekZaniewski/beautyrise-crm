name: Deploy to Vercel

on:
  push:
    branches:
      - main

jobs:
  deploy-jakub4biznes:
    runs-on: ubuntu-latest
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN_JAKUB }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install -g vercel@latest
      - run: vercel --prod --yes --scope jakub4biznes-1514s-projects

  deploy-wojtektrades:
    runs-on: ubuntu-latest
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN_WOJTEK }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install -g vercel@latest
      - run: vercel --prod --yes --scope wojtektrades-4421s-projects
